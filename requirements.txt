# ===== Gọi LLM (Gemma qua FPT, endpoint OpenAI-compatible) =====
openai
langchain-core
langchain-openai

# ===== OCR =====
azure-ai-documentintelligence

# ===== Cấu hình & kiểu dữ liệu =====
pydantic
pydantic-settings
python-dotenv

# ===== Xử lý chuỗi / ngày tháng =====
rapidfuzz
unidecode
dateparser

# ===== Xử lý file =====
python-magic
python-magic-bin; sys_platform == "win32"   # Windows cần bản này để có libmagic
pypdfium2
pillow

# ===== Gọi API ELIS =====
requests
tenacity

# ===== Giao diện demo =====
gradio

# ===== App desktop trên Windows (main_app.py) =====
# Chỉ Windows. Trên Linux, keyring kéo theo SecretStorage + jeepney và đi hỏi
# D-Bus; trong container không có D-Bus nên nó hỏng. Xem src/vault.py.
keyring; sys_platform == "win32"      # cất 4 khóa bí mật vào Credential Manager
pystray; sys_platform == "win32"      # biểu tượng khay; thiếu thì app vẫn chạy

# ===== Đọc Excel (chỉ dùng lúc dựng bộ đánh giá, không dùng khi chạy job) =====
openpyxl

# ===== Test =====
pytest
